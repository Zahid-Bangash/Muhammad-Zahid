const page = () => {
  return (
    <div
      style={{
        display: "flex",
        height: "50vh",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Next js App
    </div>
  );
};

export default page;
